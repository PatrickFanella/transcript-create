# Auditable base-version inventory for image-specific PyTorch wheels.
# Dockerfiles install the matching CPU, CUDA 12.8, or ROCm 7.1 local build.
torch==2.11.0
torchaudio==2.11.0
torchcodec==0.14.0
pyannote.audio==4.0.1
