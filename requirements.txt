# Core API framework
fastapi==0.139.0
uvicorn[standard]==0.38.0

# Database
sqlalchemy==2.0.44
psycopg[binary]==3.2.10
alembic==1.17.0

# YouTube and media processing
yt-dlp==2026.7.4

# Whisper transcription backends
faster-whisper==1.2.0
# Security-audited PyPI release compatible with the pinned Python 3.11 stack
openai-whisper==20250625

# Speaker diarization (optional, requires HF_TOKEN)
pyannote.audio==4.0.1

# Core dependencies
# Audited Python 3.11 runtime pin; GPU image variants use the same version.
numpy==2.2.6
python-dotenv==1.2.2
pydantic==2.12.3
pydantic-settings==2.11.0
requests==2.33.0

# Authentication
Authlib==1.7.2
httpx==0.28.1  # Required by Authlib's Starlette async OAuth client
itsdangerous==2.2.0  # Required for session middleware

# PDF export
reportlab==4.4.4

# Metrics and monitoring
prometheus-client==0.21.0

# Rate limiting
slowapi==0.1.9

# Redis caching
redis==5.2.1

# Optional: Error tracking (set SENTRY_DSN to enable)
# sentry-sdk==2.19.2
